const multiplicationNum = 5;

for (let i = 1; i <= 10; i++) {
  const getMultiplication = multiplicationNum * i;
  // console.log(multiplicationNum + " * " + i + " = " + getMultiplication);
  console.log(`${multiplicationNum} * ${i} = ${getMultiplication}`);
}
